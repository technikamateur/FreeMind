import action
from FreeMind import config
from pzSMART import Device

class HddSpace():
    def defaultAction():
        hhdSpace = {hdd: {'total': -1, 'free': -1}}
        df = subprocess.run(sudo.append(["/bin/bash", scriptPath]).append(*scriptArgs), stdout=subprocess.PIPE).stdout.decode('utf-8')
    df.

